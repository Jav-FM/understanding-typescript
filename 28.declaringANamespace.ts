//We can create a namespace to add whatever we want,
//for example a couple of interfaces
//(We export them so we can use it in the next file)

namespace app {
  export interface MyFirstInterface {}
  export interface MySecondInterface {}
}
